global:
  sendAnonymousUsage: false

api:
  dashboard: true
  insecure: true

log:
  level: INFO
  format: common

entryPoints:
  web:
    address: :80

providers:
  file:
    directory: /etc/traefik/
  docker:
    exposedByDefault: false
